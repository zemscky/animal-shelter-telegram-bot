package com.example.animalsheltertelegrambot.models;

/**
 * this interface is needed to determine whether to send a shelter information message depending on the user's choice
 */
public interface AnimalInfoMessage {
    String getText();
}
